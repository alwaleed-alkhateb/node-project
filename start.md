#steps for start[
    #1. npm init -y
    #2. npm install express
    #3. npm install nodemon --save-dev
    #4. npm install express-validator
    -----------------------------------
    structure of project[
        controller
        data 
        route 
        packages.json
        index.js
        model
    -----------------------------------
    session 6 [
    #1. npm install mongodb-schema
    #2. npm install mongoose --save
    ----------------------------------
    #1. create a schema
    #2. create a model
    #3. connect to the database mongoose
    ----------------------------------
    session 7 [
    pagination
    j send => http status
    npm install dotenv
    ---------------------------------
    session 8 [
    #1. npm install bcryptjs => for hashing password , hash.compare ,hash.hash(req.body.password, 12);
    #2. npm install validate-email => for validating email
    #3. create a schema for user
    #3. create a router for user
    #3. create a controller for user

    session 9 [
        #1. npm install multer 
        #2. add role in user model
        #3. add avatar in user model
        #4. add verify in courses router
     
]]]]]]

git init
git add .
git commit -m "first commit"
git branch -M main
git push -u origin main
git push -u origin master


<!-- git remote add origin https://github.com/Abdulrahman-Abdulrahman/NodeJS.git
git push -u origin main -->


